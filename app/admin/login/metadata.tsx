import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Admin Login - QuickMart",
  description: "Login to access the admin dashboard",
}

export const viewport = {
  themeColor: "#ffffff",
} 